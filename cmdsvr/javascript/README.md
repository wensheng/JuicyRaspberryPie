# Javascript(Node) Command server for JuicyRaspberryPie

Programming Minecraft with javascript!

## Usage

Start the command server:

    node index.js

Create your command function inside a file in pplugins/ folder. It will be registed as a Minecraft "/p" command.
